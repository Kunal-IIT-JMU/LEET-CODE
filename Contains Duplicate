class Solution {
public:
    bool containsDuplicate(vector<int>& nums) {
        set <int> temp(nums.begin(),nums.end());
        if(temp.size()<nums.size()){
            return true;
        }
        else{
            return false;
        }
    }
};
